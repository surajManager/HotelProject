@model IEnumerable<HotelProjx.Models.Roominfo>

@{
    ViewBag.Title = "Double Rooms";
}

<h2>Double Rooms</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoomType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookingStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookingID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoomType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookingStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookingID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoomNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.RoomNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RoomNumber })
        </td>
    </tr>
}

</table>
<div>
        @Html.ActionLink("Back to Available", "Available")
    </div>
